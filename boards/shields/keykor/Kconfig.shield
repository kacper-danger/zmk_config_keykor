# No whitespace after the comma or in your part name!
config SHIELD_KEYKOR_LEFT
    def_bool $(shields_list_contains,keykor_left)

# No whitespace after the comma or in your part name!
config SHIELD_KEYKOR_RIGHT
    def_bool $(shields_list_contains,keykor_right)