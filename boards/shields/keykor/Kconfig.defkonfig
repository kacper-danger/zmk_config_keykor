if SHIELD_KEYKOR_LEFT

# Name must be less than 16 characters long!
config ZMK_KEYBOARD_NAME
    default "Keykor"

config ZMK_SPLIT_ROLE_CENTRAL
    default y

endif

if SHIELD_KEYKOR_LEFT || SHIELD_KEYKOR_RIGHT

config ZMK_SPLIT
    default y

endif